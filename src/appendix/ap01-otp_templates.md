# OTP 模板

此附录包含了 `gen_server` 与 `supervisor` 和 `application` 三个模板的完整列表。这些模板已被内置进 Emacs 模式。

## 通用服务器模板


```erlang
{{#include ../../projects/appendix/gen_server_template.full}}
```

## 监控器模板

```erlang
{{#include ../../projects/appendix/supervisor_template.full}}
```



## 应用模板


```erlang
{{#include ../../projects/appendix/application_template.full}}
```

